{
  "name": "suibasecampworkshop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check functions/api/agents.js && node --check lib/agents.js && node --check site/agents.js",
    "dev": "wrangler pages dev site",
    "deploy": "wrangler pages deploy site --project-name suibasecampworkshop --branch main"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
